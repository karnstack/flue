web/dist/
web/node_modules/
flued
# `go build ./cmd/flue` drops the binary here. Anchored so it can't shadow
# the cmd/flue package directory.
/flue
# Where `make build` puts it.
/bin/
# goreleaser writes snapshot and release output here.
/dist/
.DS_Store
.superpowers/
# Deploy-time copy of scripts/install.sh; `make site-deploy` stages it.
/site/public/install.sh
# Wrangler's local cache from site deploys.
/site/.wrangler/
