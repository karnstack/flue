node_modules/
dist/
.wrangler/
# Deploying the *hosted* relay means copying the built web bundle into the
# assets directory first (docs/SAAS.md, "Deploy"), because that origin is what
# serves a browser its terminal. Only these two files are ours; everything else
# that lands here is build output and must not be committed.
public/*
!public/_headers
!public/index.html
